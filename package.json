{
  "name": "fixturefreeze",
  "version": "1.0.0",
  "description": "Copy files into fixtures/ and later check they still match.",
  "bin": {
    "fixturefreeze": "./src/cli.js"
  },
  "main": "./src/index.js",
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
